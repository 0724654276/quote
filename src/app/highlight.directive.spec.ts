import { HighlightDirective } from './highlight.directive';

describe('Highlight.Directive', () => {
  it('should create an instance', () => {
    expect(new Highlight.Directive()).toBeTruthy();
  });
});
